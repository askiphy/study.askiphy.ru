---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time class="myText" datetime={date.toISOString()}>
	{
		date.toLocaleDateString('ru-ru', {
			year: 'numeric',
			month: 'long',
			day: 'numeric',
		})
	}
</time>
